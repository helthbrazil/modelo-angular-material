import { VermelhoDirective } from './vermelho.directive';

describe('VermelhoDirective', () => {
  it('should create an instance', () => {
    const directive = new VermelhoDirective();
    expect(directive).toBeTruthy();
  });
});
